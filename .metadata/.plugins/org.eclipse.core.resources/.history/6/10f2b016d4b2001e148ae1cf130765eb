<%@ page language="java" contentType="text/html; charset=UTF-8" pageEncoding="UTF-8"%>

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Error - Nothing in Cart</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #fbd45d; /* Warm yellow background */
            margin: 0;
            padding: 0;
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100vh;
            text-align: center;
        }

        #error-message {
            background-color: #ffebc1; /* Light orange background */
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            animation: fadeInUp 1s ease; /* Add fade-in animation */
        }

        h1 {
            color: #e67e22; /* Dark orange text */
            margin-bottom: 20px;
            animation: bounce 1s forwards; /* Change infinite to forwards for single time bounce */
        }

        p {
            color: #555;
            font-size: 16px;
            animation: fadeIn 1s ease; /* Add fade-in animation */
        }

        .back-to-restaurants-btn {
            display: inline-block;
            margin-top: 20px;
            padding: 10px 20px;
            background-color: #e67e22; /* Dark orange button background */
            color: #fff;
            text-decoration: none;
            border-radius: 5px;
            transition: background-color 0.3s ease;
        }

        .back-to-restaurants-btn:hover {
            background-color: #d35400; /* Slightly darker orange on hover */
        }

        @keyframes bounce {
            0%, 20%, 50%, 80%, 100% {
                transform: translateY(0);
            }
            40% {
                transform: translateY(-20px);
            }
            60% {
                transform: translateY(-10px);
            }
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
            }
            to {
                opacity: 1;
            }
        }

        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
    </style>
</head>
<body>
    <div id="error-message">
        <h1>Oops!</h1>
        <p>It seems like your cart is empty. Add items to the cart or explore restaurants.</p>
        <a href="<%= request.getContextPath() %>/Restaurants" class="back-to-restaurants-btn">Back to Restaurants</a>
    </div>
</body>
</html>
